<Page x:Class="NoteMaster.Views.Pages.HomePage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="HomePage">


    <Border CornerRadius="5" Background="{DynamicResource SecundaryBackgroundColor}">
        <!--<Label Content="笔记" Foreground="Black" FontSize="50" FontWeight="Bold" VerticalAlignment="Center" HorizontalAlignment="Center"/>-->
        <ListView x:Name="noteList" ItemsSource="{Binding Notes}" Margin="20" Background="Transparent" BorderThickness="0" ItemContainerStyle="{StaticResource lstStyle}">
            <!--让ListView控件本身的点击效果失效-->
            


            <ListView.ItemTemplate>
                <DataTemplate>
                    <!--为笔记卡片添加点击事件-->
                    <Border Background="White" Margin="0,0,0,10" Padding="16" CornerRadius="6" MouseLeftButtonDown="NoteCard_Click"
                    Cursor="Hand">
                        <StackPanel>
                            <TextBlock Text="{Binding Title}" FontSize="18" FontWeight="Bold" TextTrimming="CharacterEllipsis"/>
                            <TextBlock Text="{Binding CreatedAt, StringFormat='{}{0:MM月dd日}'}" FontSize="12" Foreground="#888" Margin="0,4,0,0"/>
                            <TextBlock Text="{Binding Content}" FontSize="14" Foreground="#444" MaxHeight="60" TextTrimming="CharacterEllipsis" Margin="0,4,0,0"/>
                        </StackPanel>
                    </Border>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Border>
</Page>
